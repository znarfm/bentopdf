pid /etc/nginx/tmp/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    types {
        application/javascript mjs;
    }
    default_type application/octet-stream;

    gzip_static on;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 9;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json application/wasm application/x-javascript text/x-component;

    server {
        listen 8080;
        listen [::]:8080;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;
        absolute_redirect off;

        include /etc/nginx/security-headers.conf;


        location ^~ /pdfjs-viewer/ {
            try_files $uri =404;
            expires 1y;
        }
        location ^~ /pdfjs-annotation-viewer/ {
            try_files $uri =404;
            expires 1y;
        }

        if ($request_uri ~ ^/ua(/.*)?$) {
            return 301 /uk$1;
        }

        if ($request_uri ~ ^/tools/$) {
            return 301 /tools;
        }
        if ($request_uri ~ ^/tools/([a-z0-9][a-z0-9-]+?)(?:\.html)?(\?.*)?$) {
            return 301 /$1$2;
        }

        if ($request_uri ~ ^/(getting-started|contributing)(\?.*)?$) {
            return 301 /docs/$1$2;
        }

        if ($request_uri ~ ^/self-hosting(/.*)?$) {
            return 301 /docs/self-hosting$1;
        }

        if ($request_uri ~ ^/docs/(.+?)/index\.html(\?.*)?$) {
            return 301 /docs/$1/$2;
        }
        if ($request_uri ~ ^/docs/index\.html(\?.*)?$) {
            return 301 /docs/$1;
        }
        if ($request_uri ~ ^/docs/(.+?)\.html(\?.*)?$) {
            return 301 /docs/$1$2;
        }

        if ($request_uri ~ ^/docs/tools/pdf-to-docx/?(\?.*)?$) {
            return 301 /docs/tools/pdf-to-word$1;
        }
        if ($request_uri ~ ^/docs/tools/decrypt-pdf/?(\?.*)?$) {
            return 301 /docs/tools/unlock-pdf$1;
        }
        if ($request_uri ~ ^/docs/tools/encrypt-pdf/?(\?.*)?$) {
            return 301 /docs/tools/protect-pdf$1;
        }

        if ($request_uri ~ ^(/(?:en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja))?/index\.html(\?.*)?$) {
            return 301 $1/$2;
        }
        if ($request_uri ~ ^(/(?:en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja))?/pdf-to-docx(?:\.html)?/?(\?.*)?$) {
            return 301 $1/pdf-to-word$2;
        }
        if ($request_uri ~ ^(/(?:en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja))?/decrypt-pdf(?:\.html)?/?(\?.*)?$) {
            return 301 $1/unlock-pdf$2;
        }
        if ($request_uri ~ ^(/(?:en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja))?/encrypt-pdf(?:\.html)?/?(\?.*)?$) {
            return 301 $1/protect-pdf$2;
        }

        if ($request_uri ~ ^(/(?:en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja))?/([a-z0-9][a-z0-9-]*)\.html(\?.*)?$) {
            return 301 $1/$2$3;
        }

        if ($request_uri ~ ^(/(?:en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja))/([a-z0-9][a-z0-9-]*)/(\?.*)?$) {
            return 301 $1/$2$3;
        }
        if ($request_uri ~ ^/((?!(?:en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja|docs|blog)/)[a-z0-9][a-z0-9-]*)/(\?.*)?$) {
            return 301 /$1$2;
        }

        location ~ /src/pages/ {
            return 404;
        }

        location ~ ^/(en|ar|be|da|de|es|fr|id|it|ko|nl|pt|ru|sk|sv|tr|vi|zh|zh-TW|uk|ja)(/.*)?$ {
            try_files $uri $uri/ $uri.html =404;
            expires 5m;
        }

        location ~* \.html$ {
            expires 1h;
        }

        location ~* /libreoffice-wasm/soffice\.wasm\.gz$ {
            gzip off;
            types {} default_type application/wasm;
            add_header Content-Encoding gzip;
            add_header Vary "Accept-Encoding";
            include /etc/nginx/security-headers.conf;
            expires 1y;
        }

        location ~* /libreoffice-wasm/soffice\.data\.gz$ {
            gzip off;
            types {} default_type application/octet-stream;
            add_header Content-Encoding gzip;
            add_header Vary "Accept-Encoding";
            include /etc/nginx/security-headers.conf;
            expires 1y;
        }

        location ~ ^/((sw\.js|coherentpdf\.browser\.min\.js|qpdf\.wasm)$|(workers|pdfjs-viewer|pdfjs-annotation-viewer)/) {
            expires epoch;
        }

        location ~* \.(wasm|wasm\.gz|data|data\.gz)$ {
            expires 1y;
        }

        location ~* \.(js|mjs|css|woff|woff2|ttf|eot|otf)$ {
            expires 1y;
        }

        location ~* \.(png|jpg|jpeg|gif|ico|svg|webp|avif|mp4|webm)$ {
            expires 1y;
        }

        location ~* \.json$ {
            expires epoch;
        }

        location ~* \.pdf$ {
            expires 1y;
        }

        error_page 404 /404.html;

        location ^~ /docs/ {
            include /etc/nginx/security-headers-docs.conf;
            try_files $uri $uri/ $uri.html /docs/index.html =404;
            expires 5m;
        }

        location / {
            try_files $uri $uri/ $uri.html =404;
            expires 5m;
        }
    }
}
